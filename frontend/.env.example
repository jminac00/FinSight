# Backend origin. Leave empty for local dev (requests go same-origin through the
# Vite /api proxy). In production set it to the backend service URL.
VITE_API_BASE_URL=

# Development data source. Empty or unset uses MSW fixtures (no backend needed).
# Set to false to hit the real API through the Vite proxy.
VITE_ENABLE_MOCKS=
